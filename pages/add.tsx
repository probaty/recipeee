import { FunctionComponent } from "react";
import { Layout } from "../components";

const Add: FunctionComponent = (): JSX.Element => {
  return (
    <Layout title="Добавить рецепт">
      <h1>LOL</h1>
    </Layout>
  );
};

export default Add;
